<section class="on-light band-s">
  <div class="shell">
    <p class="t-label t-faint eyebrow">GYMREIGN</p>
    <h1 class="t-h1" style="max-width:12em">{{ page.title }}</h1>
    <div class="prose" style="margin-top:clamp(1.8rem,4vw,3rem)">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
