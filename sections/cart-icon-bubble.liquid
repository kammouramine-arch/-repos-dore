{%- comment -%}
  Rendered on its own by the Section Rendering API (`?sections=cart-icon-bubble`)
  so the header count updates without a page load.
{%- endcomment -%}
{%- render 'cart-bubble' -%}

{% schema %}
{
  "name": "Cart bubble"
}
{% endschema %}
